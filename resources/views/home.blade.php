@extends('layouts.app') <!--El layout la plantilla que se repite en toda la paiugna web dentro de layouts esta el archuvo app-->

@section('content') <!--Aqui va todo lo especifico de la pagina web -->
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">Dashboard</div>

                <div class="card-body">
                    @if (session('status'))
                        <div class="alert alert-success" role="alert">
                            {{ session('status') }}
                        </div>
                    @endif

                    You are logged in!
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
